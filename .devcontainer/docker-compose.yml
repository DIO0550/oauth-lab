services:
  # DevContainer用のサービス（開発時に使用）
  devcontainer:
    build:
      context: ..
      dockerfile: .devcontainer/node/Dockerfile
    volumes:
      - ..:/workspace:cached
    command: sleep infinity
